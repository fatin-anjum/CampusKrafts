PORT=5000
JWT_SECRET=campuskrafts_super_secret_jwt_key_2026
NODE_ENV=development

# Database Configuration
# Provide your MySQL credentials below.
DB_NAME=campuskrafts
DB_USER=root
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=3306

# Email Configuration (Optional - for sending real emails via OTP)
# If empty, OTPs will be printed to the backend console log for convenience.
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@campuskrafts.com
